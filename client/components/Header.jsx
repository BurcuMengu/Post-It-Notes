import React from "react";

function Header() {
    return(
        <header>
            <div>
                <button>Login</button>
                <button>Sign Up</button>
            </div>
        </header>
    );
}

export default Header();